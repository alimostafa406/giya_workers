import assert from 'node:assert/strict';import test from 'node:test';import{buildWorkerControlAlerts}from'./src/utils/workerControlCenter.js';
const w={id:'w',full_name:'Worker',employee_code:'1',team_id:'t',team_name:'Team',is_active:true,staff_classification:'normal'};const rows=(abs)=>['2026-09-21','2026-09-22','2026-09-23','2026-09-24'].map((d,i)=>({worker_id:'w',attendance_date:d,status:abs.includes(i)?'absent':'present'}));
test('control center alerts deterministic absence thresholds and exclusions',()=>{let a=buildWorkerControlAlerts({workers:[w],attendance:rows([0,1,2]),today:'2026-09-24',weekStart:'2026-09-21',monthStart:'2026-09-01'});assert(a.some(x=>x.type==='consecutive_absence'&&x.severity==='critical'));assert(a.some(x=>x.type==='weekly_absence'));const special={...w,id:'s',staff_classification:'special_staff'};assert.equal(buildWorkerControlAlerts({workers:[special],attendance:[],today:'2026-09-24',weekStart:'2026-09-21',monthStart:'2026-09-01'}).length,0)});
test('inactive punch and activated today are read-only alerts',()=>{const inactive={...w,id:'i',is_active:false};const a=buildWorkerControlAlerts({workers:[inactive],events:[{worker_id:'i',event_timestamp:'2026-09-24T08:00:00Z'}],activated:[w],today:'2026-09-24',weekStart:'2026-09-21',monthStart:'2026-09-01'});assert(a.some(x=>x.type==='inactive_punch'));assert(a.some(x=>x.type==='activated_today'))});
