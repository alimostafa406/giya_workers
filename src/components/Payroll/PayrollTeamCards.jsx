import { useTranslation } from '../../i18n/LanguageContext'
import { payrollTeamId } from '../../utils/payrollTeamSelection'

export default function PayrollTeamCards({ groups = [], onOpenTeam }) {
  const { t } = useTranslation()
  return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3" data-payroll-team-cards>{groups.map((group) => <button key={group.id} type="button" aria-label={group.name} className="surface-card group flex min-h-36 flex-col justify-between overflow-hidden border-2 border-transparent p-5 text-start shadow-sm transition duration-200 hover:-translate-y-0.5 hover:border-(--primary) hover:shadow-lg focus-visible:border-(--primary) focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-200" onClick={() => onOpenTeam(payrollTeamId(group.id))}><span className="flex w-full items-start justify-between gap-4"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-lg font-black text-(--primary)">{String(group.name || '?').trim().charAt(0)}</span><span className="status-badge status-badge--neutral">{group.lines.length} {t('payroll.workers')}</span></span><span className="mt-5 flex w-full items-end justify-between gap-3"><strong className="text-lg leading-snug">{group.name}</strong><span className="shrink-0 text-sm font-extrabold text-(--primary) group-hover:underline">{t('payroll.open')}</span></span></button>)}</div>
}
